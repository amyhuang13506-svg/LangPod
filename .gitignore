# Xcode
build/
DerivedData/
*.xcuserstate
*.xcuserdatad/
xcuserdata/

# Pipeline output (generated content, not code)
pipeline/output/
pipeline/logs/

# OS
.DS_Store
Thumbs.db

# API keys are in config.py — DO NOT commit
# pipeline/config.py is tracked but keys should be rotated before open-sourcing
pipeline/config.py
pipeline/config.py.local

# Umeng SDK (vendored binaries, linked via FRAMEWORK_SEARCH_PATHS, not committed)
iOS/

# App Store submission materials (local only)
审核截图/
描述截图/
docs/douyin_batch/
docs/douyin_cards_preview.html
